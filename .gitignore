# Never commit these — they contain session cookies, personal state, creds, and deps.
profile/
node_modules/
libs/
chats.json
projects.json
autoreview.json
.env
*.env
!.env.example
!*.env.example
.lock

# ChatGPT Sources sync artifacts — repo snapshots for Project Sources (local only, contains .git + metadata)
# Narrow pattern: only root probe zips, not all *.zip in subdirs
/agy-workflow_*.zip
/agy-workflow-*.zip
/opencode-workflow_*.zip
/opencode-workflow-*.zip
/agy-workflow_*.md
/opencode-workflow_*.md
.chatgpt-sources/
.sources-tracking.json
tracking-last-version.json

# OS / editor
.DS_Store
*.log
